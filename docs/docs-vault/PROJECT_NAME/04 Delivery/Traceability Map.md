# Traceability Map

> Write the CONTENT in your project's output language. This is a skeleton — fill the rows.

Maps each backlog item back to the product and architecture sources that justify it. Every active task should trace to at least a PRD and (where relevant) a Flow and an Architecture delta.

| Ticket ID | PRD source | Flow source | Architecture source | Notes |
|-----------|-----------|-------------|----------------------|-------|
| `<TICKET-ID>` | `PRD - <FEATURE_AREA>.md` | `Flow - <FLOW_NAME>.md` | `Architecture Delta.md §<n>` | `<short note>` |
| `<TICKET-ID>` | `PRD - <FEATURE_AREA>.md` | `<— / n.a.>` | `<— / n.a.>` | `<short note>` |
